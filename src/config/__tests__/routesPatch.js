/* eslint-disable no-undef */
import * as routesPatch from '../routesPatch';

describe('Routes patch constants', () => {
  it('Should be defined path ADMIN_PANEL_BASE_PATH', () => {
    expect(routesPatch.ADMIN_PANEL_BASE_PATH).toBeDefined();
  });
  it('Should be defined path ADMIN_PANEL_BASE_PATH_PREFIX', () => {
    expect(routesPatch.ADMIN_PANEL_BASE_PATH_PREFIX).toBeDefined();
  });

  it('Should be defined path LOGIN_PATH', () => {
    expect(routesPatch.LOGIN_PATH).toBeDefined();
  });

  it('Should be defined path FORGOT_PASSWORD_PATH', () => {
    expect(routesPatch.FORGOT_PASSWORD_PATH).toBeDefined();
  });

  it('Should be defined path RESET_PASSWORD_PATH', () => {
    expect(routesPatch.RESET_PASSWORD_PATH).toBeDefined();
  });

  it('Should be defined path ARTISTS_PATH', () => {
    expect(routesPatch.ARTISTS_PATH).toBeDefined();
  });

  it('Should be defined path ARTWORK_PATH', () => {
    expect(routesPatch.ARTWORK_PATH).toBeDefined();
  });

  it('Should be defined path CONSIGNMENTS_PATH', () => {
    expect(routesPatch.CONSIGNMENTS_PATH).toBeDefined();
  });

  it('Should be defined path CONTACTS_PATH', () => {
    expect(routesPatch.CONTACTS_PATH).toBeDefined();
  });

  it('Should be defined path INVOICES_PATH', () => {
    expect(routesPatch.INVOICES_PATH).toBeDefined();
  });

  it('Should be defined path REPORTS_PATH', () => {
    expect(routesPatch.REPORTS_PATH).toBeDefined();
  });

  it('Should be defined path MARKETING_PATH', () => {
    expect(routesPatch.MARKETING_PATH).toBeDefined();
  });

  it('Should be defined path WEBSITES_PATH', () => {
    expect(routesPatch.WEBSITES_PATH).toBeDefined();
  });

  it('Should be defined path MEDIA_LIBRARY', () => {
    expect(routesPatch.MEDIA_LIBRARY).toBeDefined();
  });

  it('Should be defined path EXIBITIONS_PATH', () => {
    expect(routesPatch.EXIBITIONS_PATH).toBeDefined();
  });

  it('Should be defined path RESOURCES_PATH', () => {
    expect(routesPatch.RESOURCES_PATH).toBeDefined();
  });
  it('Should be defined path QUICK_SALE_PATH', () => {
    expect(routesPatch.QUICK_SALE_PATH).toBeDefined();
  });
  it('Should be defined path VISITOR_TODAY_PATH', () => {
    expect(routesPatch.VISITOR_TODAY_PATH).toBeDefined();
  });
  it('Should be defined path SURVEY_RESULTS_PATH', () => {
    expect(routesPatch.SURVEY_RESULTS_PATH).toBeDefined();
  });
  it('Should be defined path ACCOUNT_EDIT_PATH', () => {
    expect(routesPatch.ACCOUNT_EDIT_PATH).toBeDefined();
  });
  it('Should be defined path USERS_LIST_PATH', () => {
    expect(routesPatch.USERS_LIST_PATH).toBeDefined();
  });
  it('Should be defined path USERS_RESET_PASSWORD_PATH', () => {
    expect(routesPatch.USERS_RESET_PASSWORD_PATH).toBeDefined();
  });
});
